---
title: "Slug Test"
pubDate: 2025-11-12
description: "Failure Reslience with Proxmox"
draft: false
---

If [Murphy's Law](https://en.wikipedia.org/wiki/Murphy%27s_law) tells us that anything that can go wrong will go wrong,
then we must consider anything that can go wrong when planning failure resilience for Proxmox nodes and guests.
For each type of failure, what is the length of the service outage it creates?

Link to post on Proxmox for Homelabs.

Motivation was to adopt virtualization to modernize and simplify administration of network services.
Perhaps less commmon for a homelab, I wanted to focus on failure resilience to minimize downtime.

Values for high-end homelabs likely overlap substantially with smaller-scale IT shops or startups.

Should use term "SMBs" above with smaller-scale IT shops.

Use Samsung Evo 990 Plus as a base. High quality consumer SSD with good performance and reasonable price.
No need for Evo 990 Pro since NAS host CPU will have plenty of RAM.


## Storage Volume Manager and Filesystem

ZFS is clearly the best choice for local storage. Ceph is a possibility for distributed storage, but overkill for homelabs.

Assumption: Wouldn't use storage failure resilience on PBS with off-site baackups too.

Assumption: Always use ZFS mirror for boot.

Need to mention difference between NAS and compute workloads.

Dual NVMe to USB-C enclosure: https://www.amazon.com/ORICO-Dual-M-2-NVMe-SATA/dp/B0B6NXFJK7/

I'm done with spinning drives, noisy fans, and high power consumption 24x7.
So I'm staying with compact, low TDP CPUs, and M.2 NVMe storage.

You're likely to spend more on storage than on compute at the low end no matter what you do,
so I think it's best to pay a bit more for name-brand drives with good warranties and reliability records.
I've stayed with the Samsung 990 line, but beware of the confusing naming and features.

Samsung has the 990 EVO, 990 EVO Plus, and 990 Pro, each costing a bit more than the other.
The 990 EVO Pro has a dedicated DRAM cache, but I don't see that adding
a lot of value behind the large DRAM requirements of ZFS.
The slightly higher performance of the 990 EVO Plus likely makes no difference given the
limited CPU power of low-TDP CPUs and their network interface bottlenecks.
So I'd stay with the 990 EVO drives unless you need the 4T density that's only available in the EVO Plus.

## Proxmox Node Failure Scenarios, Avoidance, and Recovery

## Compute Failure

The physical hardware, other than storage, underlying a Proxmox node fails. Loss of power, kernel panic, CPU bursts into flames, etc.

## Network Connectivity Failure

The node's physical hardwware is fine, but it cannot communicate with clients or other nodes.

### Network Link Failure

The node's physical hardware is fine and the network switch is fine, but the physical link connecting them fails.

Configure bonded interfaces with LACP using multiple physical links to a switch.

### Network Switch Failure

The node's physical hardware is fine, but the network switch it connects to fails.

Configure multiple interface with MLAG using links to different switches.

## Boot Device Failure

Proxmox node can't boot because the boot device fails.

Configure boot device as RAID1 ZFS or LVM mirrored.

### Feather In Somewhere

🚨 5. Important gotchas for ZFS stripe
	•	You still get ZFS’s end-to-end checksums, copy-on-write, snapshots, and compression.
	•	But a single disk failure destroys the whole pool.
	•	Scrubs will still run and detect corruption, but can’t repair it.
	•	Backups or ZFS replication are your only real data protection.


## Backup and Storage Design

### On-Site Backup Needs Touchy

* Fat finger recovery
* Local storage failure recovery
  * E.g. RAID0 with on drive failed or RAIDZ with two drives failed

#### Off-Site Backup Needs

* Local storage failure with local backup failure
* Site disaster recovery

### On-Site Storage Needs

#### Tier 1

* ZFS with ECC RAM
* Replicated HA boot
* Single-drive storage failure resilient

#### Tier 2

* ZFS with Non-ECC RAM
* Replicated HA boot
* Single-drive storage failure vulnerable

## Table Test

| Guest    | Node Boot | Storage | Example |
|----------|:-----|---------:|:-----:|
| Tier 1 | RAID1 ZFS w/ECC RAM, Backed up, Replicated, HA | Automated node failover in seconds | Critical network services |
| Tier 2 | RAID1 ZFS w/Non-ECC RAM, Backed up      | Manual node failure recovery | DVR, chain, fah |
| Tier 3 | RAID1 ZFS                 | Row 3 Col 3 | XXX |

* Node boots to run guest
* Guest boots to serve clients
* Some guests may access non-boot bulk storage, typically > double-digit GB
* Nodes dump to backup servers
* Baseline: RAID1 ZFS Non-ECC boot for nodes and backup servers

* XXX Boot failure resilience:
* XXX Storage failure resilience:
